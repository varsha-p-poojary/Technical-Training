#include<stdio.h>
#include<math.h>
int main()
{
    int n,i=1;
    printf("Enter a number: ");
    scanf("%d",&n);
    while(i<=n)                            //to print n natural numbers
    {
        printf("%d\n",i);
        i++;
    }
    return 0;
}

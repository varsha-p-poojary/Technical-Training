#include<stdio.h>
#include<math.h>
int main()
{
   float a;
    printf("Enter the number whose square root has to be found: ");
    scanf("%f",&a);
    printf("The square of %f is %f",a,sqrt(a)); 
return 0;
}
